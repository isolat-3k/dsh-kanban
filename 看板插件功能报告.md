# Hermes 看板（Kanban）插件功能报告

> 基于源码分析：`apps/desktop/src/plugins/kanban/`（桌面端 UI）+ `plugins/kanban/dashboard/plugin_api.py`（后端 REST API）+
> `~/.hermes/kanban.db`（SQLite 数据层）
> 生成日期：2026-08-14

---

## 1. 插件是什么

Hermes 桌面应用内置的 **任务看板插件**，是全套「多代理协作任务板」的图形界面（GUI）。

它连接三层结构：

```
┌─────────────────────────────────────────────┐
│  桌面端 UI（/kanban 页面、侧边栏、状态栏）      │
│  apps/desktop/src/plugins/kanban/            │
├─────────────────────────────────────────────┤
│  REST API（/api/plugins/kanban）             │
│  plugins/kanban/dashboard/plugin_api.py     │
├─────────────────────────────────────────────┤
│  SQLite 数据库（~/.hermes/kanban.db）         │
│  tasks / task_links / task_comments /        │
│  task_events / task_runs / task_attachments  │
└─────────────────────────────────────────────┘
```

同一份数据库同时被三处驱动：桌面插件（GUI）、`hermes kanban` 命令行、以及**调度器（dispatcher）**——它会自动把「Ready」状态的任务分派给对应 profile 的 Hermes 代理去执行。换句话说，**看板不只是待办清单，而是一个真实的「派单 → 干活 → 汇报」自动化流水线**。

⚠️ 插件默认是**关闭**的（`defaultEnabled: false`）。需要在 **Settings → Plugins** 里手动开启，开启后侧边栏才会出现 Kanban 入口。

---

## 2. 全局入口（不止一个页面）

| 入口 | 说明 |
|---|---|
| `/kanban` 页面 | 插件主页面，看板本体 |
| 侧边栏导航 | 左侧导航栏的 "Kanban" 项，点击直达看板 |
| 状态栏计数徽章 | 右下角状态栏显示 **"N"（running+ready 数）**，实时反映有多少任务正在跑/待跑；有活动时才显示，点击直达看板 |
| 命令面板（⌘K） | 两条命令：`Kanban: Open board` 打开看板；`Kanban: New task` 新建任务 |
| 快捷键 | **⌘⌥N / Ctrl+Alt+N** 从任意界面直接弹出新建任务对话框（先跳转看板再打开新建窗口） |

插件支持 **四语言界面**：英文 / 简体中文 / 繁体中文 / 日文。

---

## 3. 看板主界面

### 3.1 九列状态泳道

任务按状态分布在九列中，每一列有主题色：

```
Triage → Todo → Scheduled → Ready → Running → Blocked → Review → Done → Archived
```

- **Triage**：待细化任务（需要人来写清楚规格）
- **Scheduled**：定时任务（需要调度器唤醒时间）
- **Ready**：就绪待派发——调度器会在下一个 tick（默认 60 秒）把它派给指定 profile 的代理执行
- **Running**：代理正在干活
- **Blocked**：被卡住（等人/等依赖/缺权限）
- **Review**：干完等审核
- **Done / Archived**：完成与归档

### 3.2 任务卡片

每张卡片显示：

- **标题**（两行截断）
- **摘要**（最新运行总结或任务正文）
- **状态色条**（左侧边框按列着色）
- **页脚**：负责代理的头像/名字徽章、卡片年龄、运行时钟
- **活动弧线动画**：只有当代理**真正在跑这个任务**时，卡片边框才有一圈流动动画；若代理心跳超过 2 分钟消失（可能已挂），弧线变琥珀色提示「stale」。没在跑的任务不假装在跑。

交互方式：

- **拖拽换列**：直接把卡片拖到目标列（有拖影预览；系统锁定列不能拖入，拖出是可以的）
- **右键菜单**：打开 / 选择 / 移动到任意列 / 删除
- **Ctrl/Cmd+点击**：多选卡片 → 顶部出现批量操作条
- **点击卡片**：打开右侧任务详情抽屉
- **列折叠**：点击列头可折叠整列
- **空列自动隐藏**：全新看板才显示完整结构，有内容后空列自动收起

### 3.3 筛选

列头上方有筛选菜单（kebab）：按 **标题 / 正文 / 任务 ID** 全文搜索过滤。

### 3.4 批量操作条

多选后出现，可批量：**改状态（移动）、分配负责人、删除**。操作后计数实时修正，不会误导。

### 3.5 新建任务对话框

点击任意列头「+」或按 ⌘⌥N 打开，字段包括：

- 标题 / 描述（正文）
- **负责人（Assignee）**：指定执行任务的 profile；不选 = parked（停泊，明确不跑）
- **工作区类型**：scratch（临时目录）/ dir（指定共享目录）/ worktree（git 工作树）
- **项目**（可选，绑定项目目录）
- **父任务**：设为某任务的子任务，父任务完成前子任务不会派发
- **Goal mode**：让代理进入目标循环——干完由裁判模型判定「确实完成了」才算完
- **优先级**、**Triage 标记**（进 Triage 列待细化）
- **工作量预估**（可选）：点击后调用辅助模型，根据标题+正文估算大致工作量（耗 token，需显式确认）

新建时若调度器未在运行，界面会给出「该任务将闲置不动」的诚实警告。

---

## 4. 任务详情抽屉（点卡片展开）

右侧抽屉是任务的完整控制台：

- **状态控制按钮**（Jira 风格）：彩色按钮显示当前状态，点击弹出所有可迁移状态（带列色圆点），一键转换
- **负责人菜单**：查看/更换执行 profile
- **描述区**：完整任务正文，可编辑
- **评论线程**：`CommentComposer` 发评论。**运行中的代理会轮询评论线程，几秒内就能看到新留言**——不用停任务就能给代理递话
- **「Note & requeue」按钮**：对 Running 中的任务，发一条评论 + 触发 reclaim（召回重跑），让代理带着新上下文从头重跑——一键替代「阻塞 → 评论 → 解阻塞」的繁琐流程
- **附件**：上传 / 下载 / 删除附件（存 `task_attachments` 表）
- **工作量预估**：显示/重算估算
- **诊断面板**（Diagnostics）：运行诊断信息
- **事件时间线**：完整的事件流水（创建、派发、心跳、阻塞、完成……全部有记录）
- **运行历史**：每次运行的详情，可查看运行 inspect 信息、**可远程终止一个正在跑的 worker**（terminate）

---

## 5. 多看板支持

右上角看板切换器（BoardSwitcher）支持**多个看板并存**：

- **创建新看板**（NewBoardDialog）
- **切换看板**：切换后所有查询/缓存干净隔离，状态栏徽章计数跟着变
- **看板设置**（BoardSettingsDialog）：设置**看板级默认值**——默认负责人、默认工作区目录、默认项目。新建任务不填就继承看板默认值
- **项目选择器**（ProjectPicker）：把任务绑定到项目

每个看板对应一个独立数据库文件（`~/.hermes/kanban/boards/<slug>/kanban.db`），当前看板由 `~/.hermes/kanban/current` 指向。

---

## 6. 编排面板（Orchestration）

- 查看/修改**编排设置**：默认负责人、orchestrator profile 等全局派发规则
- **Profile 名册**：列出所有可用执行 profile 及其描述
- **自动描述**（auto-describe）：点击后让模型自动为某个 profile 生成描述（耗 token，显式触发）

---

## 7. 后端 API 全景（REST，前缀 /api/plugins/kanban）

| 类别 | 端点 |
|---|---|
| 看板 | GET /board、GET/POST /boards、PATCH/DELETE /boards/{slug}、POST /boards/{slug}/switch |
| 任务 | GET/POST /tasks、GET/PATCH/DELETE /tasks/{id}、POST /tasks/bulk（批量） |
| 依赖 | POST/DELETE /links（父→子门禁） |
| 评论 | POST /tasks/{id}/comments |
| 附件 | GET /tasks/{id}/attachments、GET/DELETE /attachments/{id} |
| 运行 | GET /runs/{id}、GET /runs/{id}/inspect、POST /runs/{id}/terminate（杀进程） |
| 生命周期 | POST /tasks/{id}/reclaim（召回重跑）、/specify（细化 triage 卡）、/reassign（换人）、/decompose（自动分解为子任务） |
| 预估 | POST /estimate、POST /tasks/{id}/estimate |
| 派发 | POST /dispatch（手动踢一脚调度器）、GET /workers/active、GET /diagnostics、GET /stats |
| 配置与选项 | GET /config、GET /assignees、GET /model-options、GET /projects |
| 通知订阅 | GET /home-channels、POST/DELETE /tasks/{id}/home-subscribe/{platform}（任务动态推送到消息平台） |
| 编排 | GET/PUT /orchestration、GET /profiles、PATCH /profiles/{name}、POST /profiles/{name}/describe-auto |
| 日志 | GET /tasks/{id}/log |

---

## 8. 实时性机制（三层保障）

1. **WebSocket 事件流**（socket）：任何任务变动立即推送刷新，看板与状态栏徽章实时更新
2. **60 秒心跳轮询**：socket 不可用时的兜底（OAuth 远程、连接断开等场景）
3. **Nudge dispatcher**：界面上**每次编辑都会防抖地踢一下调度器**——新建一个 Ready 任务，worker 立刻被派发，不用等下一个 60 秒 tick

---

## 9. 底层数据模型（kanban.db）

- `tasks`：41 个字段，涵盖 assignee、status、priority、workspace（kind/path/branch/project）、claim 锁（claim_lock/expires）、skills 技能要求、model_override 模型覆盖、goal_mode、block 原因与次数、max_runtime_seconds、last_heartbeat_at、idempotency_key 等
- `task_links`：父子依赖边（门禁派发）
- `task_comments`：评论线程
- `task_events`：不可变事件流水（审计）
- `task_runs`：每次运行记录（profile、PID、心跳、结果、错误）
- `task_attachments`：附件文件
- `kanban_notify_subs`：平台通知订阅

---

## 10. 需要知道的设计细节与限制

- **这是调度器队列，不是普通清单**：Ready + 有负责人的任务会被真实代理执行（消耗 token）。测试卡要尽快归档，否则真的会花钱跑代理
- **锁定列**：部分系统状态（如 archived）不能拖入；`scheduled` 需要调度器或 CLI 附加唤醒时间，裸拖状态会被 409 拒绝
- **parked（不分配）**：卡片明确不跑；但若看板配了默认负责人，Ready 的未分配任务会在调度器下一个 tick 被自动分配
- **失败重试**：连续派发失败 N 次（默认 2）任务自动转 Blocked 浮出水面
- **重复阻塞**：同一任务反复因同一原因阻塞会被自动升级到 Triage
- **blocked 语义**：`dependency` 型阻塞在依赖完成后自动恢复；`needs_input`/`capability` 型需要人来处理
- **每任务可钉技能/模型**：创建时可指定 worker 必须加载的 skill 与模型覆盖，派发时生效
- **幂等创建**：`idempotency-key` 防止自动化重复建卡
- **与 CLI 完全同源**：桌面 UI、`hermes kanban` 命令、调度器共享同一个数据库，任何一端操作其他端立即可见

---

## 11. 一句话总结

**看板插件 = 多代理任务流水线的可视化驾驶舱**：人建卡、拖状态、写评论、看诊断、杀进程；调度器自动把 Ready 卡派给对应 profile 的代理执行；代理的心跳、运行历史、失败原因全部实时呈现在卡片和抽屉里——从「派活」到「验收」全流程可视化，支持多看板、多语言、批量操作、依赖门禁与工作量预估。
